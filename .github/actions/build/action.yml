name: 'Build'
description: 'Builds the project into an executable and runs tests'

runs:
  using: 'composite'

  steps:
    - run: |
        mkdir build && cd build
        cmake ..
        make
        make test
